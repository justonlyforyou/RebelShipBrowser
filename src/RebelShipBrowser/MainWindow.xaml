<Window x:Class="RebelShipBrowser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="RebelShip Browser"
        Height="900" Width="1400"
        Background="{StaticResource BackgroundColor}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinHeight="600" MinWidth="800"
        Icon="Resources/icon.ico"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        StateChanged="Window_StateChanged">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Collapsed Header Toggle (visible when header is hidden) -->
        <Border x:Name="HeaderToggle" Grid.Row="0"
                Background="{StaticResource CardBackground}"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1"
                Padding="0,4" Cursor="Hand"
                MouseLeftButtonDown="HeaderToggle_Click"
                Visibility="Collapsed">
            <TextBlock Text="v" FontSize="12" FontWeight="Bold"
                      Foreground="{StaticResource TextSecondary}"
                      HorizontalAlignment="Center"/>
        </Border>

        <!-- Full Header (auto-hides after 10s) -->
        <Border x:Name="HeaderFull" Grid.Row="0" Background="{StaticResource CardBackground}"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1"
                Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="RebelShip Browser" FontSize="14" FontWeight="Bold"
                                  Foreground="{StaticResource TextColor}" VerticalAlignment="Center"/>
                        <TextBlock x:Name="VersionText" Text="" FontSize="10"
                                  Foreground="{StaticResource TextSecondary}"
                                  VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                    <TextBlock FontSize="10" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0">
                        <Run Text="by "/><Hyperlink NavigateUri="https://github.com/justonlyforyou/rebelship-browser"
                                                   RequestNavigate="Hyperlink_RequestNavigate"
                                                   Foreground="{StaticResource PrimaryColor}">justonlyforyou</Hyperlink>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,10,0">
                    <Button x:Name="RefreshButton" Content="Refresh"
                           Click="RefreshButton_Click"
                           Background="{StaticResource PrimaryColor}"
                           Foreground="White" BorderThickness="0"
                           Padding="12,6" FontSize="11"
                           Cursor="Hand" Margin="0,0,8,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                   CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button x:Name="RestartLoginButton" Content="Re-Login"
                           Click="RestartLoginButton_Click"
                           Background="{StaticResource WarningColor}"
                           Foreground="White" BorderThickness="0"
                           Padding="12,6" FontSize="11"
                           Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                   CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Hide Header Button -->
                <Button x:Name="HideHeaderButton" Grid.Column="2" Content="^"
                       Click="HideHeaderButton_Click"
                       Background="Transparent"
                       Foreground="{StaticResource TextSecondary}"
                       BorderThickness="0"
                       Padding="8,4" FontSize="12" FontWeight="Bold"
                       Cursor="Hand" ToolTip="Hide toolbar">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                               CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- WebView2 Browser -->
        <Border Grid.Row="1" Background="White">
            <wv2:WebView2 x:Name="WebView" />
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.Row="1"
               Background="#CC0f172a" Visibility="Visible">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="LoadingText" Text="Extracting session from Steam..."
                          FontSize="18" Foreground="{StaticResource TextColor}"
                          HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                            Foreground="{StaticResource PrimaryColor}"/>
            </StackPanel>
        </Border>

        <!-- Footer Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource CardBackground}"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0"
                Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Status Icon -->
                <Ellipse x:Name="StatusIndicator" Grid.Column="0"
                        Width="8" Height="8" Fill="{StaticResource WarningColor}"
                        VerticalAlignment="Center" Margin="0,0,10,0"/>

                <!-- Status Text -->
                <TextBlock x:Name="StatusText" Grid.Column="1"
                          Text="Initializing..."
                          FontSize="11" Foreground="{StaticResource TextSecondary}"
                          VerticalAlignment="Center"/>

                <!-- Minimize hint -->
                <TextBlock Grid.Column="2"
                          Text="Close to minimize to tray"
                          FontSize="10" Foreground="{StaticResource TextSecondary}"
                          VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
